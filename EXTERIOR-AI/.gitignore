node_modules/

# Real keys — never commit. .env.example is the template.
.env
.env.local

# Captured homeowner leads and detection logs (contains personal data).
data/*.jsonl

# Runtime daily-usage counter for the paid endpoints.
data/usage.json
