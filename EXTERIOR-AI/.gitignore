node_modules/

# Real keys — never commit. .env.example is the template.
.env
.env.local

# Captured homeowner leads and detection logs (contains personal data).
data/*.jsonl

# Runtime daily-usage counter for the paid endpoints.
data/usage.json

# Real surveyed properties — contains addresses. The committed template is
# data/survey-samples.example.json.
data/survey-samples.json
